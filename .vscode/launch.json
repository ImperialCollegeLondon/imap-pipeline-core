{
    "configurations": [

        {
            "console": "integratedTerminal",
            "justMyCode": false,
            "name": "Python: Debug Tests",
            "program": "${file}",
            "purpose": [
                "debug-test"
            ],
            "request": "launch",
            "type": "debugpy"
        },
        {
            "code": "import prefect_server.workflow; prefect_server.workflow.deploy_flows(local_debug=True)",
            "cwd": "${workspaceFolder}",
            // vscode will load .env file by default - so copy defaults.env to .env and edit locally
            //"envFile": "${workspaceFolder}/defaults.env",
            "name": "Python Debugger: Prefect deploy and run",
            "request": "launch",
            "type": "debugpy",
            //"preLaunchTask": "start_servers"
        },
        {
            "name": "Python Debugger: Current File",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            // vscode will load .env file by default - so copy defaults.env to .env or tweak the line below to point to your env file
            //"envFile": "${workspaceFolder}/defaults.env",
            //"preLaunchTask": "start_servers"
        },
    ],
    "version": "0.2.0"
}
